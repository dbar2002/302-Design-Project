import 'package:flutter/material.dart';

//These may change depending on what looks best, but at least we have a base
const smallerTextSize = 10.0;
const directionInstructionSize = 17.0;
const regularTextSize = 13.0;
const titleSize = 17.0;
const headingSize = 15.0;

const smallerTextColor = Colors.blueGrey;
const directionInstructionColor = Colors.black;
const regularTextSizeColor = Colors.black;
const titleSizeColor = Colors.black;
const headingSizeColor = Colors.black;
